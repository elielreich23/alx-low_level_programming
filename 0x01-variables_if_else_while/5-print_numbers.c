#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: returns 0 when everything works fine
 *
*/

int main(void)
{
	int l;

	for (l = 0; l < 10; ++l)
	{
		printf("%d", l);
	}
	putchar(10);
	return (0);
}
